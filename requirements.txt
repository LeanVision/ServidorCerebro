# Dependencias de ServerLocal.py (Cerebro).
# torchreid/ está vendorizado en el repo, no es un paquete de pip.
#
# En Raspberry Pi (ARM64) instalar sin pines exactos la primera vez: dejar que
# pip resuelva los wheels disponibles para esa arquitectura/versión de Python.
fastapi
uvicorn[standard]
python-multipart
requests
numpy
opencv-python
torch
torchvision
insightface
onnxruntime
# torchreid/__init__.py importa TODOS sus datasets al cargar el paquete
# (aunque sólo usemos FeatureExtractor); gdown y scipy son imports a nivel de
# módulo de datasets que no usamos (university1652, grid/mars/ilidsvid) y
# rompen el import si faltan.
gdown
scipy
# torchreid/engine/engine.py hace "from torch.utils.tensorboard import
# SummaryWriter" a nivel de módulo (parte del motor de entrenamiento, que no
# usamos, pero igual se importa). torch no trae tensorboard como dependencia.
tensorboard
